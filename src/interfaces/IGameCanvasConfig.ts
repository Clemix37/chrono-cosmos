export default interface IGameCanvasConfig {
    id:string; // Id of game canvas
    width:number; // Width for the game canvas
    height:number; // height for the game canvas
    bgColor:string; // Background color for the game canvas
}